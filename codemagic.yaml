workflows:
  ios_unsigned_ipa_for_ksign_from_zip:
    name: iOS unsigned IPA for KSign from ZIP
    max_build_duration: 60
    instance_type: mac_mini_m2

    environment:
      node: latest
      xcode: latest
      cocoapods: default

    scripts:
      - name: Clean workspace and unzip project
        script: |
          set -e

          echo "Repository-Inhalt:"
          ls -la

          ZIP_FILE=$(find . -maxdepth 1 -type f -name "*.zip" | head -n 1)

          if [ -z "$ZIP_FILE" ]; then
            echo "FEHLER: Keine ZIP-Datei im Hauptordner gefunden."
            exit 1
          fi

          echo "Gefundene ZIP-Datei: $ZIP_FILE"

          rm -rf extracted
          mkdir -p extracted

          unzip -q "$ZIP_FILE" -d extracted

          echo "Entpackter Inhalt:"
          find extracted -maxdepth 4 -type f | head -n 200

          PROJECT_DIR=$(find extracted -name "package.json" -exec dirname {} \; | head -n 1)

          if [ -z "$PROJECT_DIR" ]; then
            echo "FEHLER: Keine package.json in der ZIP gefunden."
            echo "Entpackter Inhalt:"
            find extracted -maxdepth 6 -type f | head -n 300
            exit 1
          fi

          echo "Projektordner gefunden: $PROJECT_DIR"
          echo "PROJECT_DIR=$PROJECT_DIR" >> "$CM_ENV"

      - name: Install npm dependencies and Capacitor
        script: |
          set -e

          cd "$PROJECT_DIR"

          echo "Node Version:"
          node -v

          echo "NPM Version:"
          npm -v

          npm install

          npm install @capacitor/core @capacitor/cli @capacitor/ios --save-dev

          echo "Capacitor Version:"
          npx cap --version

      - name: Ensure Capacitor config exists
        script: |
          set -e

          cd "$PROJECT_DIR"

          if [ ! -f "capacitor.config.json" ] && [ ! -f "capacitor.config.ts" ]; then
            echo "capacitor.config fehlt. Erstelle capacitor.config.json..."

            cat > capacitor.config.json <<'EOF'
          {
            "appId": "de.hartung.heatcurvenexus",
            "appName": "Heat Curve Nexus",
            "webDir": "www",
            "backgroundColor": "#111318",
            "ios": {
              "backgroundColor": "#111318",
              "contentInset": "never",
              "scrollEnabled": true,
              "allowsLinkPreview": false
            }
          }
          EOF
          fi

          echo "Capacitor Config:"
          cat capacitor.config.json || true

      - name: Ensure www folder exists
        script: |
          set -e

          cd "$PROJECT_DIR"

          if [ ! -d "www" ]; then
            echo "www-Ordner fehlt. Suche index.html..."

            INDEX_DIR=$(find . -name "index.html" -exec dirname {} \; | head -n 1)

            if [ -z "$INDEX_DIR" ]; then
              echo "FEHLER: Keine index.html gefunden."
              exit 1
            fi

            echo "index.html gefunden in: $INDEX_DIR"

            mkdir -p www

            shopt -s dotglob
            cp -R "$INDEX_DIR"/* www/
          fi

          echo "www-Inhalt:"
          ls -la www | head -n 80

      - name: Create and sync Capacitor iOS project
        script: |
          set -e

          cd "$PROJECT_DIR"

          rm -rf ios

          echo "Erstelle iOS-Projekt..."
          npx cap add ios

          echo "Synchronisiere iOS-Projekt..."
          npx cap sync ios

          echo "iOS-Struktur:"
          find ios -maxdepth 5 -type f | head -n 200

      - name: Patch native iOS WebView scroll bounce
        script: |
          set -e

          cd "$PROJECT_DIR"

          if [ -f "scripts/patch-ios-webview-scroll.sh" ]; then
            echo "Führe nativen iOS Scroll/Bounce Patch aus..."
            bash scripts/patch-ios-webview-scroll.sh
          else
            echo "FEHLER: scripts/patch-ios-webview-scroll.sh fehlt."
            echo "Vorhandene scripts-Dateien:"
            find . -maxdepth 4 -type f -path "*scripts*" || true
            exit 1
          fi

      - name: Install CocoaPods if Podfile exists
        script: |
          set -e

          cd "$PROJECT_DIR"

          if [ -f "ios/App/Podfile" ]; then
            echo "Installiere CocoaPods..."
            cd ios/App
            pod install
            cd ../..
          else
            echo "Kein Podfile gefunden. CocoaPods wird übersprungen."
          fi

          echo "Xcode-Dateien:"
          find ios -maxdepth 6 \( -name "*.xcworkspace" -o -name "*.xcodeproj" \) -print

      - name: Build unsigned iOS app
        script: |
          set -e

          cd "$PROJECT_DIR"

          WORKSPACE_PATH=$(find ios -name "App.xcworkspace" | head -n 1)
          PROJECT_PATH=$(find ios -name "App.xcodeproj" | head -n 1)

          if [ -n "$WORKSPACE_PATH" ]; then
            echo "Baue mit Workspace: $WORKSPACE_PATH"

            xcodebuild \
              -workspace "$WORKSPACE_PATH" \
              -scheme App \
              -configuration Release \
              -sdk iphoneos \
              -derivedDataPath build/ios \
              CODE_SIGNING_ALLOWED=NO \
              CODE_SIGNING_REQUIRED=NO \
              CODE_SIGN_IDENTITY="" \
              build

          elif [ -n "$PROJECT_PATH" ]; then
            echo "Baue mit Project: $PROJECT_PATH"

            xcodebuild \
              -project "$PROJECT_PATH" \
              -scheme App \
              -configuration Release \
              -sdk iphoneos \
              -derivedDataPath build/ios \
              CODE_SIGNING_ALLOWED=NO \
              CODE_SIGNING_REQUIRED=NO \
              CODE_SIGN_IDENTITY="" \
              build

          else
            echo "FEHLER: Keine App.xcworkspace oder App.xcodeproj gefunden."
            echo "iOS-Inhalt:"
            find ios -maxdepth 6 -type f | sort
            exit 1
          fi

      - name: Package unsigned IPA for KSign
        script: |
          set -e

          cd "$PROJECT_DIR"

          APP_PATH=$(find build/ios/Build/Products/Release-iphoneos -maxdepth 2 -type d -name "*.app" | head -n 1)

          if [ -z "$APP_PATH" ]; then
            echo "FEHLER: Keine .app gefunden."
            echo "Build-Inhalt:"
            find build/ios/Build/Products -maxdepth 6 -type d || true
            exit 1
          fi

          echo "Gefundene App: $APP_PATH"

          rm -rf Payload artifacts
          mkdir -p Payload artifacts

          cp -R "$APP_PATH" "Payload/HeatCurveNexus.app"

          /usr/bin/zip -qry "artifacts/HeatCurveNexus-v1.1.0-unsigned-for-KSign.ipa" Payload

          echo "IPA erstellt:"
          ls -la artifacts

    artifacts:
      - extracted/**/artifacts/*.ipa
      - extracted/**/build/ios/Build/Products/Release-iphoneos/*.app
      - /tmp/xcodebuild_logs/*.log
